import { Outlet } from "react-router";

const HostCreateTeamFormLayout: React.FC = () => {
  return (
    <div>
      <p>Create Team Form Layout</p>
      <Outlet />
    </div>
  );
};

export default HostCreateTeamFormLayout;
